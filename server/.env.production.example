# Backend runtime template for production hosting.
# Copy this file to server/.env.production when you want the backend to load it
# automatically with NODE_ENV=production, or point SERVER_ENV_FILE at a different path.

HOST=127.0.0.1
PORT=8787
TIMD_FRONTEND_BASE_URL=https://department.example.edu/
AUTH_SESSION_COOKIE_NAME=timd_session
AUTH_SESSION_TTL_SECONDS=28800
SERVER_STATE_DB_FILE=/var/lib/thisismydepartment/state.sqlite

TIMD_DEFAULT_ORGANIZATION=Example University
TIMD_DEFAULT_DEPARTMENT=Industrial Engineering
TIMD_DEFAULT_ROOM_ID=industrial-engineering-main
TIMD_DEFAULT_ROOM_DISPLAY_NAME=Industrial Engineering Department

AUTH_ALLOW_INSECURE_DEV_HANDOFF=false

# Pick one primary auth mode and configure only the variables it needs.

# Shared-secret POST handoff
# AUTH_HANDOFF_SHARED_SECRET=replace-me

# JWT handoff
# AUTH_JWT_SHARED_SECRET=replace-me
# AUTH_JWT_ISSUER=https://sso.example.edu
# AUTH_JWT_AUDIENCE=thisismydepartment

# Reverse-proxy header handoff
# AUTH_PROXY_PROVIDER=campus-sso
# AUTH_PROXY_EXTERNAL_ID_HEADER=x-user-id
# AUTH_PROXY_DISPLAY_NAME_HEADER=x-display-name
# AUTH_PROXY_EMAIL_HEADER=x-user-email
# AUTH_PROXY_ORGANIZATION_HEADER=x-user-org
# AUTH_PROXY_DEPARTMENT_HEADER=x-user-department
# AUTH_PROXY_ROLES_HEADER=x-user-roles
# AUTH_PROXY_AUTHENTICATED_HEADER=x-authenticated
# AUTH_PROXY_AUTHENTICATED_VALUE=true

# Embedded iframe or popup auth bridge
# AUTH_POSTMESSAGE_ALLOWED_ORIGINS=https://portal.example.edu

# Avatar LLM routing.
# If TIMD_AGENT_LLM_PROVIDER is unset, the backend prefers OPENROUTER_API_KEY,
# then OPENAI_API_KEY, and falls back to mock mode when neither is configured.
# TIMD_AGENT_LLM_PROVIDER=openrouter

# OpenRouter
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=openai/gpt-4.1-mini
# OPENROUTER_APP_NAME=ThisIsMyDepartment.AI
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1/

# OpenAI
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4.1-mini
# OPENAI_BASE_URL=https://api.openai.com/v1/

# Mock fallback
# MOCK_LLM_MODEL=local-context-preview